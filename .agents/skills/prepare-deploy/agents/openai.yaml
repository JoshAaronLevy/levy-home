interface:
  display_name: "Prepare Deploy"
  short_description: "Build and verify a Transporter-ready IPA"
  default_prompt: "Use $prepare-deploy to create a verified Levy Home Transporter release."
policy:
  allow_implicit_invocation: false
